name: Deploy (manual)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show manifests (demo)
        run: |
          echo "This is where we would deploy to Kubernetes."
          ls -la
          cat k8s.yaml
